deploy_renoir() {
    local target_group=${1:-all}
    ansible-playbook -i ansible/inventory.ini ansible/deploy_distributed.yaml \
        -e renoir_executable=../target/debug/examples/groups \
        -e renoir_target_group=$target_group
}